// ComboCardWrapper.jsx
import React from 'react';
import ComboCard from './Comobos/Combos';

const ComboCardWrapper = ({ userId, combination }) => {
  return <ComboCard userId={userId} combination={combination} />;
};

export default ComboCardWrapper;
